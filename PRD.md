# 扫描鸡 · 产品需求文档 (PRD v1.0)

> **产品宣言：更快、更准确、更便宜**
> 对标：夸克扫描王（UI/交互风格）、CamScanner（功能对比基准）
> 目标：MVP 上线 App Store，iOS 16.0+

---

## 一、产品定位

| 维度 | 描述 |
|------|------|
| 核心用户 | 需要偶发性扫描/OCR/PDF 处理的普通用户 |
| 竞争策略 | 更简洁的产品结构 + 更低的价格（12元/年 vs 竞品百元以上） |
| 差异化 | 端侧 AI 识别（无需联网）+ 无繁杂功能噪音 |
| 应用商店名称 | **扫描鸡** |
| Bundle ID | `com.saomiaoji.app`（待注册后确认） |

---

## 二、信息架构

```
扫描鸡
├── [首页] 扫描主界面          ← 默认 Tab
├── [历史] 文档历史记录
└── [我的] 账号 + 会员 + 设置
```

### 导航规范（对标夸克扫描王）
- 底部 TabBar，3 个 Tab，图标 + 文字标签
- 主色调：**深蓝色 #1A73E8**（或 #0066FF），简洁纯色风格
- 卡片圆角：16pt；按钮圆角：12pt
- 字体：系统默认（SF Pro / PingFang SC）
- 背景：浅灰白 `#F5F5F7`，卡片白色 `#FFFFFF`

---

## 三、页面详细需求

### 3.1 首页 · 扫描主界面

#### 布局结构
```
┌─────────────────────────────┐
│  扫描鸡              [用户头像] │  ← NavigationBar
├─────────────────────────────┤
│                             │
│  ┌─────────┐  ┌─────────┐  │
│  │  📷 拍照  │  │  🖼 相册  │  │  ← 主操作区（上方居中）
│  └─────────┘  └─────────┘  │
│                             │
│  ┌───────────────────────┐  │
│  │  📄 导入 PDF 文件      │  │  ← PDF 导入（次要操作）
│  └───────────────────────┘  │
│                             │
│     [使用提示 / 空状态插图]   │
│     点击上方按钮开始识别      │
└─────────────────────────────┘
```

#### 交互细节
- **拍照按钮**：调用系统相机，支持自动对焦、闪光灯控制
- **相册导入**：支持多选图片（最多 10 张），批量识别排队处理
- **PDF 导入**：调用文件选择器（`UIDocumentPickerViewController`），仅支持 `.pdf`
- 识别进行时：显示全屏 Loading 蒙层 + 进度动画（转圈 + "识别中…"文案）
- 识别完成后：自动跳转至【文字面板】

---

### 3.2 文字面板（识别结果页）

> 识别完成后弹出的全屏 Sheet 或 Push 页面

#### 布局结构
```
┌─────────────────────────────┐
│  ← 返回        文字面板      │  ← NavigationBar
├─────────────────────────────┤
│                             │
│  [原始图片缩略图 · 可点击放大] │
│                             │
├─────────────────────────────┤
│  识别结果                    │
│                             │
│  ┌───────────────────────┐  │
│  │                       │  │
│  │  （识别出的文字内容）   │  │  ← 可滚动文本区域
│  │   长按选择部分文字      │  │
│  │                       │  │
│  └───────────────────────┘  │
│                             │
│  [非中文时显示翻译按钮 ↓]    │
│  ┌───────────────────────┐  │
│  │  🌐 翻译为中文简体      │  │
│  └───────────────────────┘  │
│                             │
├─────────────────────────────┤
│  [ 复制全文 ]  [ 导出 Word ] │  ← 底部操作栏（固定）
└─────────────────────────────┘
```

#### 交互细节

| 操作 | 行为 |
|------|------|
| 长按文本 | 系统原生文字选择器（支持拖动选区 + 复制） |
| 点击「复制全文」 | 全文复制到剪贴板，顶部弹出 Toast："已复制到剪贴板 ✓" |
| 点击「导出 Word」 | 生成 .docx 文件 → 弹出系统分享面板（可保存到文件/发送到微信等） |
| 点击「翻译为中文」| 调用翻译 API（或 Apple Translation Framework），在文本区下方展示译文；按钮变为「收起译文」 |
| 非中文语言 | 自动检测，显示翻译按钮；中文内容不显示 |
| 水印（非会员）| 导出的 Word 文档首页页眉添加水印文案 |

**水印文案**：
> 扫描图文、多语言翻译、pdf转word就用 极速扫描app

**文字面板不保留历史**：关闭后内容不存储，仅历史页存储 Word 文件记录。

---

### 3.3 PDF 转换流程

> 用户导入 PDF → 自动提取文字 + 保留图片位置 → 生成 Word

#### 处理逻辑
1. 用 `PDFKit` 逐页渲染 PDF
2. 每页：提取嵌入文字（`PDFPage.string`）；若无文字层则对页面截图做 OCR
3. 图片元素：提取 PDF 页面内的图像，按原始位置嵌入 Word
4. 生成 Word 文档，格式尽量还原原始 PDF 布局

#### UI 流程
```
导入 PDF → 解析页数提示（共 X 页）→ 转换中（进度条）→ 完成 → 文字面板（含图片）
```

---

### 3.4 历史记录页

#### 布局结构
```
┌─────────────────────────────┐
│  历史记录                    │
├─────────────────────────────┤
│  ┌───────────────────────┐  │
│  │ 📄 识别结果_20260312   │  │
│  │ 2026年03月12日 10:30:00│  │  ← 时间格式：年月日 时:分:秒
│  │ "这是识别出的文字预览…" │  │
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │ 📄 识别结果_20260311   │  │
│  │ 2026年03月11日 15:20:00│  │
│  │ "另一条记录的预览内容…"│  │
│  └───────────────────────┘  │
│            ...              │
│                             │
│   (未登录时显示引导登录提示)  │
└─────────────────────────────┘
```

#### 规范
- 排序：按识别时间**倒序**
- 时间格式：`yyyy年MM月dd日 HH:mm:ss`（含秒）
- 每条记录存储：时间 + Word 文件路径 + 文字预览（前 200 字）
- 仅存储 Word 文件路径，**不保留**原始图片/PDF
- **不保留**文字面板内容（识别结果关闭即销毁）
- 点击记录：重新打开对应 Word 文件（系统文件预览）或提示文件已删除
- 左滑删除单条记录
- 未登录状态：显示空态插图 + "登录后查看历史记录"按钮

---

### 3.5 我的（账号页）

#### 布局结构
```
┌─────────────────────────────┐
│  我的                        │
├─────────────────────────────┤
│  ┌───────────────────────┐  │
│  │  👤 未登录             │  │
│  │  [登录 / 注册]         │  │  ← 已登录则显示手机号
│  └───────────────────────┘  │
│                             │
│  ┌───────────────────────┐  │
│  │  👑 开通会员           │  │
│  │  12元/年 · 2元/月      │  │  ← 会员卡片（突出展示）
│  │  去除水印 · 无限使用   │  │
│  └───────────────────────┘  │
│                             │
│  ─────────────────────────  │
│  📋 使用条款                 │
│  🔒 隐私政策                 │
│  ℹ️ 关于扫描鸡               │
│  ─────────────────────────  │
└─────────────────────────────┘
```

---

### 3.6 登录页

#### 流程
```
输入手机号 → 点击「获取验证码」→ 60s 倒计时 → 输入 6 位验证码 → 登录成功
```

#### 规范
- **无密码**设计，仅手机号 + 短信验证码
- 登录成功后 Session 有效期 **90 天**
- 期间任意一次打开 App：自动检测 Session → 若距过期 < 7 天则自动续期 90 天
- Session 到期后跳回登录页
- 注册 = 首次登录（无单独注册流程）

---

### 3.7 会员订阅页

#### 方案
| 档位 | 价格 | 周期 | 说明 |
|------|------|------|------|
| 月度会员 | ¥2 | 每月自动续费 | StoreKit 订阅 |
| 年度会员 | ¥12 | 每年自动续费 | **推荐**，相当于 1元/月 |

#### 规则
- 通过 **StoreKit 2** 实现 In-App Purchase 订阅
- 会员特权：**去除水印**
- 非会员：导出文件带水印（页眉文字）
- 会员标识存储在本地 + iCloud（防止卸载重装丢失）

---

## 四、技术方案

### 4.1 技术栈

| 模块 | 方案 |
|------|------|
| 开发语言 | Swift 5.9+ |
| UI 框架 | SwiftUI |
| 数据持久化 | SwiftData（iOS 17+）/ CoreData（兼容 16） |
| OCR | Vision Framework（端侧，免费，无网络依赖） |
| 语言检测 | NaturalLanguage Framework |
| 翻译 | Apple Translation Framework（iOS 17.4+）或 免费 API |
| PDF 处理 | PDFKit |
| Word 导出 | [ZIPFoundation](https://github.com/weichsel/ZIPFoundation) 手动生成 .docx（Open XML 格式） |
| 付费订阅 | StoreKit 2 |
| 手机号登录 | 自建后端短信接口（或接入第三方如极光、阿里云短信） |
| 历史记录 | SwiftData 本地存储 |

### 4.2 OCR 多语言支持

```swift
request.recognitionLanguages = [
    "zh-Hans", "zh-Hant",  // 简/繁中文
    "en",                   // 英文
    "ja", "ko",            // 日韩
    "fr", "de", "es",      // 欧洲主流
    "ar"                   // 阿拉伯文
]
request.recognitionLevel = .accurate
request.usesLanguageCorrection = true
```

### 4.3 Word (.docx) 导出实现思路

.docx 本质是 ZIP 压缩包，核心文件：
- `word/document.xml`：正文内容（Open XML 格式）
- `word/document.xml.rels`：图片关系
- `[Content_Types].xml`：文件类型声明

步骤：
1. 用 ZIPFoundation 构建 ZIP 结构
2. 将识别文字写入 document.xml
3. 嵌入图片（若有）并建立 rels 关系
4. 非会员在页眉插入水印文字

### 4.4 PDF 转 Word 实现思路

```swift
// 1. 读取 PDF
let pdfDocument = PDFDocument(url: pdfURL)

// 2. 逐页处理
for pageIndex in 0..<pdfDocument.pageCount {
    let page = pdfDocument.page(at: pageIndex)
    
    // 2a. 尝试提取内嵌文字
    let embeddedText = page.string ?? ""
    
    // 2b. 无文字层则截图 OCR
    if embeddedText.isEmpty {
        let pageImage = renderPageToImage(page)
        performOCR(on: pageImage) { text in ... }
    }
    
    // 2c. 提取图片元素（复杂，MVP 可先跳过图片还原）
}
```

> **MVP 简化方案**：PDF 转 Word 先实现文字提取，图片保留（原位置）列为 v1.1 迭代。

### 4.5 翻译方案

优先级：
1. **Apple Translation Framework**（iOS 17.4+，免费，离线）
2. 降级：调用 MyMemory 免费翻译 API（有频率限制）
3. 降级：提示用户复制文字到翻译 App

---

## 五、MVP 功能优先级

### P0 · 必须上线（MVP 核心）
- [x] App 骨架 + Tab 导航 ✅（已有代码）
- [x] 90 天 Session 管理 ✅（已有代码）
- [x] 拍照/相册导入图片 ✅（已有代码）
- [x] Vision OCR 端侧识别 ✅（已有代码）
- [ ] 文字面板 UI 完善（长按选择 + 复制全文 + 导出按钮）
- [ ] Word (.docx) 真实导出（当前只有 .txt 占位）
- [ ] 非会员水印（导出 Word 时加页眉）
- [ ] 手机号登录（短信验证码）
- [ ] 历史记录（SwiftData 持久化）
- [ ] StoreKit 2 订阅（月度 + 年度）
- [ ] 基础 UI 精修（对标夸克扫描王风格）

### P1 · 上线后第一个迭代
- [ ] PDF 转 Word（文字提取）
- [ ] 翻译功能（Apple Translation）
- [ ] 批量图片识别
- [ ] iCloud 会员状态同步

### P2 · 未来版本
- [ ] PDF 转 Word（图片位置保留）
- [ ] 手写识别优化
- [ ] Android 版本

---

## 六、数据模型（最终版）

```swift
// SwiftData 持久化模型
@Model
final class ScanRecord {
    @Attribute(.unique) var id: UUID
    var createdAt: Date            // 识别时间
    var wordFilePath: String       // Word 文件本地路径
    var wordFileSize: Int64        // 文件大小（字节）
    var textPreview: String        // 文字预览（前 200 字）
    var detectedLanguage: String   // 语言代码 e.g. "zh-Hans"
    
    // 格式化时间：yyyy年MM月dd日 HH:mm:ss
    var formattedDate: String { ... }
}
```

---

## 七、权限声明（Info.plist）

| 权限 | 用途说明 |
|------|---------|
| NSCameraUsageDescription | 拍摄文档进行文字识别 |
| NSPhotoLibraryUsageDescription | 从相册选择图片进行文字识别 |
| NSPhotoLibraryAddUsageDescription | 保存扫描结果到相册（可选） |

---

## 八、上架准备 Checklist

- [ ] Apple 开发者账号注册（99 USD/年）
- [ ] App Store Connect 创建 App 记录
- [ ] Bundle ID 注册：`com.saomiaoji.app`
- [ ] 隐私政策 URL（已有 `PrivacyPolicy.md`，需托管到线上）
- [ ] 使用条款 URL
- [ ] App 截图（iPhone 6.7" / 6.1" / 5.5"）
- [ ] App 图标（1024x1024 PNG）
- [ ] App 描述文案（中文 + 英文）
- [ ] StoreKit 产品 ID 配置
- [ ] 短信服务接入（手机号登录后端）

---

## 九、开发环境

```
Xcode 16.0+
iOS Deployment Target: 16.0
Swift 5.9+
XcodeGen（项目文件生成）
```

### 推荐安装的辅助工具

```bash
# XcodeGen（已在项目中使用）
brew install xcodegen

# ZIPFoundation（Word 导出，通过 Swift Package Manager 添加）
# URL: https://github.com/weichsel/ZIPFoundation

# xcbeautify（编译日志美化，可选）
brew install xcbeautify
```

---

_PRD v1.0 · 生成于 2026-03-12 · 阿晨_
